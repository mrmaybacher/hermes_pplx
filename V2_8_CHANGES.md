# Hermes v2.8 Changes

- Checklist parsing now supports list markers on their own line, including numbered and bullet markers, using the next non-empty line(s) as item text while preserving inline list parsing and normalized-text dedupe behavior.
